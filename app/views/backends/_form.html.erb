<%= form_with(model: backend, local: true, html: {class: 'space-y-4 w-full md:space-y-8 md:w-2/3'}) do |form| %>
  <% if backend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backend.errors.count, "error") %> prohibited this backend from being saved:</h2>

      <ul>
        <% backend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="vlc-input">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="vlc-input">
    <%= form.label :subdomain %>
    <%= form.text_field :subdomain %>
  </div>

  <%= form.submit class: 'vlc-button primary' %>

<% end %>
