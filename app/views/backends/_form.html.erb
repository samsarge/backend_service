<%= form_with(model: backend, local: true) do |form| %>
  <% if backend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backend.errors.count, "error") %> prohibited this backend from being saved:</h2>

      <ul>
        <% backend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :subdomain %>
    <%= form.text_field :subdomain %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
